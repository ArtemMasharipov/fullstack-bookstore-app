# План рефакторинга фронтенда Fullstack Bookstore App

## 📋 Контекст проекта

**Технологии:**

- Vue 3.2.13 (Composition API)
- Pinia 3.0.2 (state management)
- Vue Router 4.0.3
- Vuetify 3.8.3
- Axios 1.7.9
- JavaScript (без TypeScript)

**Цель:** Улучшить архитектуру, читаемость и поддерживаемость кода по best practices Vue 3.

---

## 🎯 ЭТАПЫ РЕФАКТОРИНГА

### ЭТАП 0: Подготовка и аудит (1-2 дня)

**Задачи:**

1. ✅ Настроить ESLint + Prettier для автоформатирования
2. ✅ Запустить линтер и автофикс (`npm run lint -- --fix`)
3. ✅ Проанализировать текущую структуру проекта
4. ✅ Создать документ ARCHITECTURE.md с описанием текущего состояния
5. ✅ Составить список всех найденных проблем

**Результат:** База для рефакторинга, единый code style

---

### ЭТАП 1: Quick Wins - Быстрые улучшения (2-3 дня)

**Задачи:**

1. **Удаление Dead Code**

   - Найти и удалить неиспользуемые импорты
   - Удалить закомментированный код
   - Удалить неиспользуемые переменные/функции

2. **Магические значения → Константы**

   ```javascript
   // Создать /src/constants/*.js
   // Вынести все магические числа/строки в константы
   ```

3. **Улучшение именования**

   - Boolean: `is*/has*/can*` префиксы
   - Функции: глаголы (`fetch*`, `handle*`, `update*`)
   - Переменные: существительные (`user`, `currentPage`)
   - Компоненты: PascalCase

4. **Добавление JSDoc**
   - Документировать сложные функции
   - Добавить описание props в компонентах

**Результат:** Чище код, понятнее без комментариев

---

### ЭТАП 2: Структурный рефакторинг (5-7 дней)

#### 2.1 Оптимизация структуры проекта

**Текущая структура требует улучшений:**

```
/src
  /components
    /features     ✅ Хорошо
    /layout       ✅ Хорошо
    /ui           ✅ Хорошо
  /composables    ✅ Хорошо (есть)
  /store          ⚠️ Нужна оптимизация
  /services       ✅ Хорошо
  /utils          ✅ Хорошо
  /router         ✅ Хорошо
```

**Задачи:**

- Проверить нет ли избыточных абстракций в `/store/utils`
- Убедиться что composables переиспользуются
- Создать `/constants` для магических значений

#### 2.2 Аудит и оптимизация Composables

**Проверить существующие composables:**

- `useAuth.js` - нужен ли или можно прямо store?
- `useCart.js` - переиспользуется или только в 1 месте?
- `useBooks.js` - есть ли дублирование с store?
- `usePagination.js` - универсальный или специфичный?
- `useForm.js` - правильная ли абстракция?

**Критерии:**

- ✅ Используется в 2+ местах → оставить
- ❌ Используется в 1 месте → удалить, логику в компонент
- ⚠️ Дублирует store → выбрать что-то одно

#### 2.3 API Layer - проверка архитектуры

**Уже есть хорошая структура:**

```
/services/api
  - baseApi.js         ✅ Axios instance
  - authService.js     ✅ Сервис авторизации
  - booksApi.js        ✅ API книг
  - ...
```

**Задачи:**

1. Убедиться что все API вызовы идут через сервисы (не прямо axios)
2. Проверить interceptors в baseApi.js:
   - ✅ Добавление токена к запросам
   - ✅ Глобальная обработка ошибок 401/403/404/500
3. Убрать дублирование между API сервисами

**Результат:** Централизованная работа с API

---

### ЭТАП 3: Рефакторинг Pinia Stores (3-5 дней)

#### 3.1 Аудит текущих stores

**Проблема:** Используется `createBaseStore` factory - избыточная абстракция?

**Stores в проекте:**

- `auth` - авторизация
- `books` - книги
- `cart` - корзина
- `orders` - заказы
- `authors` - авторы
- `users` - пользователи
- `ui` - UI состояние
- `authUi`, `ordersUi`, `usersUi`, `authorsUi` - отдельные UI stores

**Вопросы:**

1. Зачем отдельные `*Ui` stores? Можно ли объединить?
2. `createBaseStore` - упрощает или усложняет?
3. Что реально должно быть в store, что локально в компоненте?

#### 3.2 Задачи рефакторинга stores

1. **Упростить архитектуру**

   - Убрать `createBaseStore` factory если избыточен
   - Писать stores напрямую через `defineStore`
   - Объединить `*Ui` stores с основными stores

2. **Правило: что в store, что нет**

   ```
   В STORE:
   ✅ Данные используемые в 2+ компонентах
   ✅ Данные переживающие размонтирование компонента
   ✅ Данные синхронизированные с бэкендом

   НЕ в store:
   ❌ Локальное состояние компонента (isModalOpen, formData)
   ❌ Временные UI состояния
   ❌ Вычисляемые значения (используй computed в store)
   ```

3. **Структура идеального store:**

   ```javascript
   export const useAuthStore = defineStore('auth', () => {
     // STATE
     const user = ref(null)
     const token = ref(localStorage.getItem('token'))

     // GETTERS (computed)
     const isAuthenticated = computed(() => !!token.value)
     const userName = computed(() => user.value?.name || 'Guest')

     // ACTIONS
     const login = async (credentials) => { ... }
     const logout = () => { ... }

     return {
       user, token, isAuthenticated, userName,
       login, logout
     }
   })
   ```

**Результат:** Простые, понятные stores без избыточных абстракций

---

### ЭТАП 4: Рефакторинг компонентов (1-2 недели)

#### 4.1 Аудит компонентов

**Найти проблемные компоненты:**

```bash
# Компоненты больше 200 строк
find src/components -name "*.vue" -exec wc -l {} \; | sort -rn
```

**Критерии проблемного компонента:**

- > 200 строк кода
- Много разной логики (API вызовы + валидация + UI)
- Используется только в 1 месте, но сложный
- Куча props/events

#### 4.2 Паттерны разбиения

**Пример: BookList.vue (206 строк)**

Разбить на:

- `BookList.vue` - главный (композиция)
- `BookCard.vue` - карточка книги
- `BookFilters.vue` - фильтры
- `BookPagination.vue` - пагинация

**Логику вынести в:**

- Composable `useBooksList()` - загрузка, фильтрация, пагинация
- Store `useBooksStore` - только данные из API

#### 4.3 Props Validation

**Добавить валидацию:**

```javascript
defineProps({
  book: {
    type: Object,
    required: true,
    validator: value => value.id && value.title,
  },
  size: {
    type: String,
    default: 'medium',
    validator: v => ['small', 'medium', 'large'].includes(v),
  },
})
```

#### 4.4 ref vs reactive - правильное использование

**Правило:**

```javascript
// ✅ ref для примитивов
const count = ref(0)
const name = ref('')
const isActive = ref(true)

// ✅ reactive для объектов форм
const form = reactive({
  email: '',
  password: '',
})

// ✅ ref для массивов (удобнее)
const items = ref([])
```

**Результат:** Компоненты < 150 строк, переиспользуемые, понятные

---

### ЭТАП 5: Оптимизация производительности (3-5 дней)

#### 5.1 Lazy Loading Routes

**Проверить router:**

```javascript
// ❌ ПЛОХО
import HomePage from '@/views/HomePage.vue'

// ✅ ХОРОШО
const routes = [
  {
    path: '/',
    component: () => import('@/views/public/HomeView.vue'),
  },
  {
    path: '/admin',
    component: () => import('@/views/admin/AdminDashboardView.vue'),
  },
]
```

#### 5.2 Lazy Loading Components

**Тяжелые компоненты:**

```javascript
import { defineAsyncComponent } from 'vue'

const HeavyChart = defineAsyncComponent(() =>
  import('@/components/charts/HeavyChart.vue')
)
```

#### 5.3 v-show vs v-if

**Правило:**

- `v-if` - редко переключается
- `v-show` - часто переключается (модалки, табы)

#### 5.4 Debounce для поиска

```javascript
import { useDebounceFn } from '@vueuse/core'
// или создать свой composable useDebounce
```

**Результат:** Быстрая загрузка, оптимизированный bundle

---

### ЭТАП 6: Финальная полировка (2-3 дня)

**Задачи:**

1. **Code Review**

   - Пройтись по всем изменениям
   - Проверить consistency
   - Убедиться что всё работает

2. **Документация**

   - Обновить README.md
   - Создать ARCHITECTURE.md
   - Добавить комментарии в сложных местах

3. **Cleanup**

   - Удалить неиспользуемые зависимости
   - Проверить bundle size
   - Финальный ESLint

4. **Тестирование**
   - Ручное тестирование всех фич
   - Проверка на разных устройствах

---

## 📊 ПРИОРИТИЗАЦИЯ ПРОБЛЕМ

### 🔥 P0 - Критично (исправить сейчас)

- Проблемы безопасности (если есть)
- Критические баги

### 🟠 P1 - Высокий (на этой неделе)

- Избыточные абстракции (`createBaseStore`)
- Дублирование кода
- Большие компоненты-монстры (>300 строк)

### 🟡 P2 - Средний (этот месяц)

- Отсутствие lazy loading
- Неоптимальные computed/watch
- Магические значения

### 🟢 P3 - Низкий (когда-нибудь)

- Мелкие оптимизации
- Дополнительные тесты
- Дополнительная документация

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ ПОСЛЕ РЕФАКТОРИНГА

**Архитектура:**

- [ ] Логичная структура папок
- [ ] Нет дублирования кода (DRY)
- [ ] Компоненты < 200 строк
- [ ] Один компонент = одна ответственность

**Pinia Stores:**

- [ ] Store только для shared state
- [ ] Нет UI состояния компонентов в store
- [ ] Используются computed для вычислений
- [ ] Stores < 300 строк

**Composables:**

- [ ] Используются в 2+ местах
- [ ] Именование `use*`
- [ ] Возвращают реактивные данные
- [ ] Нет дублирования со stores

**Компоненты:**

- [ ] Props с валидацией
- [ ] Правильно ref/reactive
- [ ] Логика в composables, не в компонентах
- [ ] Template "глупый" (без логики)

**API Layer:**

- [ ] Все запросы через сервисы
- [ ] Настроены interceptors
- [ ] Глобальная обработка ошибок
- [ ] Нет прямых axios вызовов в компонентах

**Производительность:**

- [ ] Lazy loading routes
- [ ] Lazy loading тяжелых компонентов
- [ ] Debounce для поиска
- [ ] v-show для частых переключений

**Code Quality:**

- [ ] ESLint без ошибок
- [ ] Единый code style (Prettier)
- [ ] JSDoc для сложных функций
- [ ] Нет магических значений
- [ ] Понятные имена переменных

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

**Метрики:**

- Средний размер компонента: **< 150 строк**
- Средний размер store: **< 250 строк**
- Bundle size: **уменьшение на 15-20%** (благодаря lazy loading)
- ESLint warnings: **0**

**Качество кода:**

- ✅ Легко найти любой файл
- ✅ Легко понять что делает код
- ✅ Легко добавить новую фичу
- ✅ Легко протестировать отдельно

**Поддерживаемость:**

- ✅ Новый разработчик разберется за 1-2 дня
- ✅ Изменение одного компонента не ломает другие
- ✅ Нет страха менять код

---

## 🚀 НАЧАЛО РАБОТЫ

**Шаг 1:** Прочитай этот план целиком

**Шаг 2:** Запусти аудит:

```bash
# Анализ структуры
tree src/ > project-structure.txt

# Найти большие компоненты
find src -name "*.vue" -exec wc -l {} \; | sort -rn > component-sizes.txt

# Проверить ESLint
npm run lint
```

**Шаг 3:** Начни с ЭТАП 0

**Шаг 4:** Двигайся последовательно по этапам

**Шаг 5:** После каждого этапа - коммит в git

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

**Vue 3 Best Practices:**

- https://vuejs.org/style-guide/
- https://vuejs.org/guide/best-practices/

**Pinia Best Practices:**

- https://pinia.vuejs.org/core-concepts/

**Composition API:**

- https://vuejs.org/guide/extras/composition-api-faq.html

---

_Этот план создан для постепенного, итеративного улучшения проекта. Не пытайся сделать всё сразу - двигайся по этапам!_
